<%= form_for(@chat_message, :html => {:class => "form-horizontal"}) do |f| %>
  <% if @chat_message.errors.any? %>
    <div id="error_explanation">
      <h3 class="block-title"><%= pluralize(@chat_message.errors.count, "error") %> prohibited this chat_message from being saved:</h3>

      <% @chat_message.errors.full_messages.each do |message| %>
        <div class="alert alert-info"><%= message %></div>
      <% end %>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :body,:class => "col-md-2 control-label" %>
    <div class="col-md-10">
    <%= f.text_field :body, :class => "form-control m-b-10", :placeholder => "body" %>
    </div>
  </div>
	<div class="form-group">
		<div class="col-md-offset-2 col-md-10">
			<%= f.submit "Submit",:class => "btn btn-info btn-sm m-t-10" %>
		</div>
	</div>
<% end %>